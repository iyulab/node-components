### High Priority

## 프로젝트 체크 리스트
[ ] 전체 `event`를 활용하는 컴포넌트들 this.emit<T>()로 `import` 정리하기
[ ] 전체 컴포넌트 주석 정리하기(`@slot`, `@csspart`, `@cssprop`, `@event` jsdoc 활용)
[ ] 전체 완료 후 `llms.txt`, `SKILL.md`파일 생성, `README.md`파일 업데이트하기

### Low Priority

## Additions

- Add `color-picker` form-component;
- Add `date-picker` form-component(calendar, range, etc.);
- Add `time-picker` form-component(clock, range, etc.);
- Add `number-input` form-component(format, decimal, negative, etc.);
- Add `password-input` form-component(mask, meter, etc.);
- Add `pin-input(otp)` form-component;
- Add `qrcode` component;
- Add `u-step-panel` component;
- Add `timeline` component;
- Add `typography` component;
