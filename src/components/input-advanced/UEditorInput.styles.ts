import { css } from 'lit';

export const styles = css`
    :host {
      width: 100%;
      font-size: 14px;
    }

    u-input-border {
      height: 10em;
      overflow: hidden;
      resize: vertical;
    }

  `;