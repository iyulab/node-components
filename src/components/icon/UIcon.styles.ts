import { css } from 'lit';

export const styles = css`
    :host {
      display: inline-flex;
      font-size: 16px;
    }

    svg {
      width: 1em;
      height: 1em;
      fill: currentColor;
    }
  `;