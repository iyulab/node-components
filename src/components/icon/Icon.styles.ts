import { css } from "lit";

export const styles = css`
  :host {
    display: inline-flex;
    font-size: inherit;
    color: inherit;
  }

  svg {
    width: 1em;
    height: 1em;
    fill: currentColor;
  }
`;