import { css } from 'lit';

export const styles = css`
    .message {
      margin: 0;
      margin-bottom: 20px;
      font-size: 14px;
      line-height: 1;
      font-weight: 300;
      font-family: var(--sl-font-sans);
    }
  `;