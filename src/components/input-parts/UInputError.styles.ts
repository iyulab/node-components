import { css } from 'lit';

export const styles = css`
    :host {
      display: inline-block;
      font-size: 0.85em;
    }

    .error {
      color: red;
      font-size: inherit;
      font-weight: 400;
      line-height: 1.5;
      padding: 2px 5px;
    }
  `;